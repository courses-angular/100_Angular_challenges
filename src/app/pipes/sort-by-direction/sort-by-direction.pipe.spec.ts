import { SortByDirectionPipe } from './sort-by-direction.pipe';

describe('SortByDirectionPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByDirectionPipe();
    expect(pipe).toBeTruthy();
  });
});
